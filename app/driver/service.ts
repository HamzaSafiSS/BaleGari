/**
 * Driver service
 * Supabase logic will be added later
 * Currently safe placeholders only
 */

export const driverService = {
  setOnlineStatus: async (status: "ONLINE" | "OFFLINE") => {
    return;
  },

  subscribeToIncomingRides: () => {
    return;
  },

  startRide: async (rideId: string) => {
    return;
  },

  endRide: async (rideId: string) => {
    return;
  },
};
